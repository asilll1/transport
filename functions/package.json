{
  "name": "joldosh-functions",
  "private": true,
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "lint": "eslint --ext .ts src/"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@google-cloud/translate": "^8.5.0",
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.1.0",
    "grammy": "^1.31.3"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
